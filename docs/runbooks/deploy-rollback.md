# Runbook: API / Worker 回滚演练

## 适用场景

API、worker、cron、admin 部署失败或 GA-0/正式发布前回滚演练。

## 处理步骤

1. 标记受影响进程和版本。
2. 暂停相关队列消费者或入口流量。
3. 在 Render/Vercel 执行上一稳定版本回滚。
4. 验证健康检查、队列积压、traceId、告警恢复。
5. 记录耗时、影响范围和证据链接。

## 失败分支

任一核心进程无法回滚时，J-01 不通过，GA-0 与正式发布不得开闸。

