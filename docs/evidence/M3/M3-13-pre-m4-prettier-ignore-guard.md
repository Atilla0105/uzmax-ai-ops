# M3-13 Pre-M4 Prettier Ignore Guard

> evidence_id: M3-13-pre-m4-prettier-ignore-guard
> milestone: M3
> spec: `docs/specs/M3-13-pre-m4-prettier-ignore-guard.md`
> status: implemented_pre_m4_governance_preparation
> created_at: 2026-06-19
> updated_at: 2026-06-19
> sensitive_data_location: none
> redaction_status: no raw customer data, Telegram payloads, screenshots, voice transcripts, raw prompts, raw completions, order IDs, phone numbers, addresses, payment data, support personal accounts, LLM keys or secrets

## Scope

This evidence records only M3 signoff 前治理 / pre-M4 governance preparation. M3 is not owner accepted, and M4 has not started.

Included:

- Added a minimal `prettier-ignore` boundary guard that freezes the current monitored code/test baseline at 89 markers.
- Added focused tests for baseline pass, existing baseline expansion fail, new monitored file fail, diff-added marker fail, docs prose ignored and total expansion fail.
- Added `guard:prettier-ignore` and wired it into local `npm run check` plus CI immediately after `format:check`.

Not included:

- M4 business capability start, production release, GA-0, real customer traffic, runtime write jail, package source cleanup, DB/schema/migration/generated DTOs, lockfile changes, app changes or full-repo marker removal.

## Start Audit

| Fact | Evidence |
|---|---|
| `pwd` | `/Users/atilla/Documents/uzmax-m3-13-pre-m4-prettier-ignore-guard` |
| `git status --short --branch` | `## codex/m3-13-pre-m4-prettier-ignore-guard...origin/main` |
| `git branch --show-current` | `codex/m3-13-pre-m4-prettier-ignore-guard` |
| root/main status | `## main...origin/main` |
| `AGENTS.md` | read before implementation |
| M3-12 spec/evidence | read before implementation |
| package/CI/guard/test context | `package.json`, `.github/workflows/ci.yml`, `scripts/guards/workspace-isolation.mjs`, `scripts/guards/worker-write-boundary.mjs`, `scripts/tests/guards.test.mjs` read before implementation |
| dependency setup | `npm ci` ran because `node_modules` was missing; pass with existing 3 high severity npm audit advisories |

## Baseline

| File | Count |
|---|---:|
| `packages/evals/src/index.ts` | 10 |
| `packages/engine/src/index.ts` | 9 |
| `packages/llm-gateway/src/index.ts` | 4 |
| `packages/db/src/index.ts` | 47 |
| `packages/db/src/m3-ai-contracts.ts` | 9 |
| `packages/capabilities/speech/src/index.ts` | 5 |
| `packages/capabilities/vision/src/index.ts` | 2 |
| `scripts/tests/m3-llm-gateway-routing-accounting-foundation.test.mjs` | 3 |
| total | 89 |

## Implementation Summary

| File | Change |
|---|---|
| `scripts/guards/prettier-ignore-boundary.mjs` | New independent guard scanning monitored code/test paths, enforcing file and total baseline, new-file blocking and `--base` diff-added marker blocking. |
| `scripts/tests/prettier-ignore-boundary.test.mjs` | New fixture tests for pass/fail semantics; marker strings are generated by concatenation to avoid self-matching. |
| `package.json` | Added `guard:prettier-ignore` and inserted it into `check` after `format:check`. |
| `.github/workflows/ci.yml` | Added CI guard step immediately after `npm run format:check` with `${{ steps.guard-base.outputs.base }}`. |
| docs | Added M3-13 spec and this evidence. |

## Validation

| Command | Result | Notes |
|---|---|---|
| `node --test scripts/tests/prettier-ignore-boundary.test.mjs` | pass | 6/6 focused tests passed. |
| `npm run guard:prettier-ignore` | pass | Current monitored baseline: 8 files, 89/89 markers. |
| `npm run guard:prettier-ignore -- --base origin/main` | pass | Diff check against `origin/main` passed for monitored source/test paths. |
| `npm run format:check` | pass | Prettier check passed for the repo after formatting the new guard script. |
| `npm run lint` | pass | ESLint passed across apps, packages and scripts. |
| `npm run guard:workspace` | pass | `workspace-isolation: ok` for linked worker worktree. |
| `UZMAX_ASSIGNED_WORKTREE=/Users/atilla/Documents/uzmax-m3-13-pre-m4-prettier-ignore-guard UZMAX_PRIMARY_ROOT=/Users/atilla/Documents/UZMAX智能运营 npm run guard:worker-boundary` | pass | Explicit assigned worker/root boundary check passed. |
| `npm run guard:pr-shape -- --base origin/main --spec docs/specs/M3-13-pre-m4-prettier-ignore-guard.md --include-worktree` | pass | Spec type `infra`, changed files 6, config 2/docs 2/source 1/test 1. |
| `git diff --check origin/main...HEAD` | pass | No whitespace errors at the time run; final post-commit diff check is also required. |
| `npm run test` | pass | 149/149 Node tests passed. |
| `npm run check` | pass | Full local gate passed: format, new guard, typecheck, lint, depcruise, jscpd, knip, forbidden/eval/doc/workspace/worker/pr-shape guards, tests, build, size and Playwright 7/7. |

## PR Hygiene Summary

| Category | Result |
|---|---|
| Changed files | 6 |
| Path categories | docs, source, test, config |
| Source changed files | 1 / budget 1 |
| New source files | 1 / budget 1 |
| External API/provider/SDK evidence | none; no external provider/SDK/connector/adapter added |
| Exceptions | none |
| Test weakening | none; no `.skip` / `.only` / `xit` / `xfail` added |

## Self Review

| Check | Result |
|---|---|
| Spec compliance | pass; changed files are within the M3-13 touch list and no package/app/DB/lock/generated/customer-data files were changed. |
| Code quality | pass; focused tests, lint and full `npm run check` passed. |
| Behavior change | no product/package source behavior change; guard/config/docs/test only. |
| Workspace isolation | root/main remained read-only and clean during implementation |

## Sensitive Data Boundary

M3-13 stores only guard code, tests, config wiring and aggregate validation evidence in git. No raw customer data, Telegram payloads, screenshots, voice transcripts, raw prompts, raw completions, order IDs, phone numbers, addresses, payment data, support personal accounts, LLM keys or secrets were added.

## Concerns

- Existing `npm ci` reported 3 high severity audit advisories; this governance slice does not modify dependencies or lockfile.
